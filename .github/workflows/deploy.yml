# Súbor: .github/workflows/android_build.yml
name: Build Android APK with Cordova

on:
  push:
    branches:
      - master # Spustí sa pri každom pushi na vetvu master

jobs:
  build_android:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout kód
      uses: actions/checkout@v4

    - name: 🔧 Nastavenie Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 🔧 Inštalácia Cordova
      run: npm install -g cordova

    # ❗ PRIDANÝ OPRAVNÝ KROK: Vytvorenie štruktúry Android
    - name: ⚙️ Pridanie platformy Android do projektu
      run: cordova platform add android

    - name: 🛠️ Príprava prostredia Android SDK
      # Nastaví Android SDK a Javu, ktoré sú potrebné pre kompiláciu
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: 🔨 Cordova Build pre Android (Debug APK)
      # Spustí skutočnú kompiláciu aplikácie
      run: cordova build android --debug

    - name: 📦 Nahranie APK
      # Uloží výsledný súbor APK, aby ste si ho mohli stiahnuť
      uses: actions/upload-artifact@v4
      with:
        path: platforms/android/app/build/outputs/apk/debug/app-debug.apk
        name: android-debug-apk-${{ github.sha }}
        retention-days: 5
        
    - name: ✅ Ukončené
      run: echo "Buildovanie APK dokončené a artefakt je pripravený na stiahnutie."
